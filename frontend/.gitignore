# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# The full-size PNGs the product shots are cut from — 60 MB of originals that
# only ever leave here as the webps under public/products. Keep them on disk,
# out of the history; git is the wrong place to store them.
art-source

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
